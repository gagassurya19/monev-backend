# Server Configuration
NODE_ENV=development
PORT=3000
HOST=localhost

# Database Configuration
DB_HOST=localhost
DB_PORT=8889
DB_NAME=moodle401
DB_USER=moodle
DB_PASSWORD=moodle

# Webhook Tokens (comma-separated list)
# Example: token1,token2,token3
WEBHOOK_TOKENS=your-webhook-token-here,another-webhook-token

# JWT Configuration
JWT_SECRET=your-jwt-secret-here
JWT_EXPIRES_IN=24h

# Logging
LOG_LEVEL=info
LOG_FILE=logs/app.log

# Security
BCRYPT_SALT_ROUNDS=10

# API Configuration
API_PREFIX=/api/v1 